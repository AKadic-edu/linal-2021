#include <mathlib/matrix.hpp>

#include "mathlib.hpp"

TEST_CASE("Matrix") {
	SECTION("multiplication") {
		SECTION("by vector") {
			// Arrange
			// Act
			// Assert
			REQUIRE(false);
		}
		SECTION("by matrix") {
			// Arrange
			// Act
			// Assert
			REQUIRE(false);
		}
	}
}
