#include <mathlib/vector.hpp>

#include "mathlib.hpp"

TEST_CASE("Vector") {
	SECTION("addition") {
		SECTION("by vector") {
			// Arrange
			// Act
			// Assert
			REQUIRE(false);
		}
	}
	SECTION("subtraction") {
		SECTION("by vector") {
			// Arrange
			// Act
			// Assert
			REQUIRE(false);
		}
	}
	SECTION("multiplication") {
		SECTION("by int scalar") {
			// Arrange
			// Act
			// Assert
			REQUIRE(false);
		}
		SECTION("by vector") {
			// Arrange
			// Act
			// Assert
			REQUIRE(false);
		}
	}
}
